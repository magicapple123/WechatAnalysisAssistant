# 变更记录

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的组织方式，并使用[语义化版本](https://semver.org/lang/zh-CN/)。Beta 阶段仍可能调整接口，但会在这里记录兼容性影响。

## 0.1.0-beta.4（仅源码）- 2026-09-22

> 发布状态：与 beta.3 相同，仅提供源码，不附带 Windows 安装包（PyAV 组件的公开
> 再分发证明问题尚未解决）。无数据库迁移；已保存的密钥与设置向后兼容。

### 新增

- 提供完全虚构的演示数据生成脚本 `scripts/seed_demo_data.py` 与基于该数据的真实
  界面截图，README 重写为面向中文用户的项目主页。
- 新增 pre-commit 配置（ruff 规则与 CI 一致），并在贡献指南中说明启用方式。

### 性能

- 聊天列表、联系人聚合与会话总数增加实例级缓存，消除逐联系人×逐分片的重复全表
  查询；消息分页总数按过滤条件缓存。
- 纯同步的重型接口（会话列表、消息分页、搜索、导出、设置密钥、自动检测等）改为
  在线程池执行，不再阻塞事件循环；异步接口中的同步重活移入工作线程。
- AI 分析的 Map/Reduce 阶段改为小规模并行请求，共享取消与总时限语义不变。
- HTML 导出改为流式写入，大幅降低大聊天的导出内存峰值。

### 修复

- 修复链接渲染正则的全局状态污染导致部分 URL 不显示为链接的问题。
- 修复切换会话后线程池复用数据库连接触发跨线程错误的问题；解密器增加并发保护。
- 修复批量导出失败被静默吞掉、导出文件与索引目录错位、同名联系人文件互相覆盖的
  问题；导出失败清单现在随接口返回。
- 修复 `/api/extract-key` 在响应中回传数据库密钥明文的问题；前端不再回显密钥。
- 表情包缓存、朋友圈媒体缓存与图片解密临时缓存增加容量上限淘汰，避免长期使用
  后无限增长。

### 变更

- 为仓库补充中文版「关于」描述与主题标签；界面截图全部由虚构演示数据生成。

## 0.1.0-beta.3（仅源码候选）- 2026-08-27

> 发布状态：本版本尚未创建 Git tag，也没有公开 Windows 安装包。第三方许可审计
> 发现 PyAV 18.0.0 Windows wheel 的 x264/x265 与 MSYS2 runtime 无法满足当前公开
> 再分发证明要求；构建门禁会阻断二进制发布。源码与本机网页版不携带该 Windows
> DLL bundle，可继续发布和使用。

### 新增

- 增加 Windows 后端、React 网页端和 Electron 桌面端的持续集成检查，以及独立的
  Windows 安装包构建/发布工作流。
- 增加贡献指南、安全报告流程、社区行为准则、依赖更新配置和 Issue / Pull Request 模板。
- 增加完整 MIT 许可证文件和统一的编辑器、换行规则。
- 增加源码敏感文件检查、本机网页版启动脚本、前端 ESLint/API 单元测试和桌面端
  Electron fuse 校验。

### 修复

- 修复本机 API 对跨站浏览器请求、非法 Host 和非回环客户端限制不足的问题，并为
  API 响应增加禁止缓存与 MIME 嗅探保护。
- 修复数据库解密在大文件上占用过多内存、失败后可能遗留部分临时文件，以及并行
  实例清理彼此临时快照的问题；补充 WAL 与临时目录回归测试。
- 强化账号切换、数据库密钥格式/归属验证和导出参数校验，避免旧账号异步结果、
  非法格式或越界路径进入当前任务。
- 修复网页端语音播放/导出地址遗漏 `server_id`、旧搜索响应覆盖新结果、账号切换后
  旧数据回写，以及加载失败缺少可见反馈的问题。
- 修复 Electron 更新状态并发、退出期间新任务进入、sidecar 超时/进程树清理和打包
  烟雾测试失败后残留进程等生命周期问题。
- 修复打包烟雾测试加载 React 后可能触发 `/api/status`、`/api/auto-detect` 并探测
  构建机真实微信数据的问题：改用专用最小 `app://` 页面，关闭账号发现，并将烟测
  后端 API 限制为 desktop health/self-test/prepare-exit。

### 改进

- 前端增加顶层错误边界、弹窗键盘焦点管理、减少动态效果支持和按需加载；缩小首次
  加载包并补充维护说明。
- Electron 默认拒绝权限请求、窗口导航和未验证外链，收窄 preload/IPC 能力，并在
  打包后读取实际 fuses 验证 Run-as-Node、Node 参数和 ASAR 完整性策略。
- `wx_key` 改为显式选择的可选增强：公开/默认构建即使本机已安装也会排除，只有已
  自行确认使用与再分发授权的维护者才能通过 `-IncludeWxKey` 构建内部版本；缺少时
  保留 PyMem 内存扫描和手动输入路径。
- 固定 PyInstaller 构建工具版本，更新桌面构建依赖与安全覆盖项，并强化构建产物、
  更新元数据、SHA-256 清单和打包后目录的验证。
- 桌面在线更新改用 `Magicapple-Coder/WechatAnalysisAssistant` GitHub provider，按
  预发布/稳定版本分别使用 `beta.yml` / `latest.yml`，并校验打包后的
  `app-update.yml` 配置。
- 统一应用版本与发布文档，补充本地开发、隐私边界和验证命令。

### 安全

- 从当前待发布源码树移除误纳入版本控制的本地密钥和诊断数据库，并扩展忽略规则与
  源码检查。此操作不会自动清除已经公开的 Git 历史；凭据若曾离开受控环境必须吊销
  并轮换，维护者还需单独评估历史清理。
- 强化源码与发布内容扫描，用于拒绝已知命名/类型的设置、密钥、数据库、日志、
  诊断数据和签名私钥；扫描是纵深防护，不能证明不存在所有敏感信息或安全漏洞。
- 增加 fail-closed 原生依赖公开再分发门禁、精确 DLL/许可证哈希审批清单和
  `THIRD_PARTY_NOTICES.md`。当前 PyAV/FFmpeg Windows bundle 未获批准，Windows
  installer 与 `v0.1.0-beta.3` tag 暂停发布；NOTICE 不会被当作 GPL 合规替代品。

### 验证边界

- 自动测试和桌面烟雾测试使用匿名夹具或临时目录；桌面烟测还禁止本地微信探测，
  且刻意不加载生产 React 页面。它不覆盖实际界面、所有微信 4.x 数据库、第三方
  模型接口、杀毒软件、代码签名和 SmartScreen 场景。
- 未配置签名凭据的 Beta 是未签名产物，可能触发 SmartScreen；正式发布仍需对具体
  CI 产物完成校验和、签名链及干净 Windows 虚拟机人工验收。

## [0.1.0-beta.2] - 2026-07-19

### 新增

- 提供 Electron / NSIS Windows 桌面壳与 PyInstaller 后端 sidecar 构建流程。
- 支持聊天与朋友圈浏览、导出、图片处理、语音转写及用户主动触发的 AI 分析。
- 提供本地优先的密钥、设置和缓存存储，以及网页开发运行方式。

[0.1.0-beta.2]: https://github.com/Magicapple-Coder/WechatAnalysisAssistant/releases/tag/v0.1.0-beta.2
