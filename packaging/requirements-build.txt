# Build-only dependencies. Runtime dependencies remain in backend/requirements.txt.
# Keep this file separate so production users never install packaging tools.
# Exact pins keep the freezer output stable across local and CI builds. Update
# both together after running scripts/verify-packaging-runtime.py and the
# packaged Electron smoke test on Windows x64 / CPython 3.12.
PyInstaller==6.22.3
pyinstaller-hooks-contrib==2026.6
